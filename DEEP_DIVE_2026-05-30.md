# Deep-Dive Audit — 2026-05-30

Systematic section-by-section audit of AI Pac-Man Lab. Successor to
`CODE_REVIEW_2026-05-17.md` (whose C/H/M/L findings are all resolved) and the
`N1–N21` follow-up findings (resolved in commits `1c2b1ca`…`0b4a71f`; tracked
only in commit messages + regression tests — indexed below).

**Priorities (per user):** correctness bugs · test coverage · features.
Refactors only where they unblock the above.

**Process:** read → list findings → pause for review → fix with tests →
`npm run typecheck && npm test` gate → commit → update this doc.

**Finding IDs:** `D<section>.<n>` (e.g. `D1.2`). Tags: `[bug]` `[cov]` `[feat]`.
Status: ☐ open · ◐ in progress · ☑ done · ✗ won't-fix.

---

## Section status

| # | Section | Files | Audit | Fixes |
|---|---------|-------|-------|-------|
| 1 | engine | prng, types, collision.test | ☑ | ◐ |
| 2 | mazes | mazes.ts | ☑ | ☑ |
| 3 | ghosts | ghostAi.ts | ☑ | ☑ |
| 4 | env | environment.ts, observation.ts | ☑ | ◐ |
| 5 | rl | qlearning, trainingController, presetBench | ☐ | ☐ |
| 6 | render | canvasRenderer.ts | ☐ | ☐ |
| 7 | ui | App.tsx, main.tsx, app.css | ☐ | ☐ |
| 8 | scripts | overnight-bench, merge-policies, *.sh | ☐ | ☐ |
| 9 | config | vite/tsconfig/vitest, CI, lint | ☐ | ☐ |

---

## Findings

### Section 1 — engine

| ID | Tag | Status | File | Finding |
|----|-----|--------|------|---------|
| D1.1 | bug | ☑ | prng.ts:16 | `int()` no guard for `<=0`/NaN bound → 0/negative index. Guarded. |
| D1.2 | cov | ☑ | prng.test.ts | New file: reproducibility, range, seed-0, `int` range/guard/coverage. |
| D1.3 | cov | ☑ | types.test.ts | New file: pins `reverseAction` (C3) + `actionToDirection` clamp (H10). |
| D1.4 | cov | ☐ | collision.test.ts | Only one test. Expand (ghost touch, edible eat, power, inBox, multi-pac, swap). **Deferred to Section 4** (env behavior). |
| D1.5 | feat | ☐ | types.ts (cross-section) | Promote `Action` to a nominal type to make direction-order bugs compile errors. Cross-section; lower priority. |

**Section 1 changes:** `prng.ts` guard; new `prng.test.ts`, `types.test.ts`.
Gate (`typecheck`+`test`) pending local run — no node toolchain in audit sandbox.

### Section 2 — mazes

| ID | Tag | Status | File | Finding |
|----|-----|--------|------|---------|
| D2.1 | bug | ☑ | mazes.ts | pacStart coincided with a corner power pellet → `powerPelletPositions` over-reported (env drops it). **Conservative fix**: drop the pacStart anchor; zero gameplay change (env already placed only 3) — preserves training baseline. |
| D2.2 | bug | ☑ | mazes.ts:findOpenNear | Fallback could return a wall tile. Now scans whole interior for any open tile before giving up. |
| D2.3 | cov | ☑ | mazes.test.ts | Reachability flood-fill: all open tiles reachable from pacStart. |
| D2.4 | cov | ☑ | mazes.test.ts | Spawn-tiles-passable + even→odd dimension bump tests. |
| D2.5 | feat | ☑ | mazes.ts | Exported `validateMaze(m): string[]`; run across all MAZES in tests + a negative test. Subsumes D2.1–D2.4 as a reusable guard. |
| D2.6 | feat | ☐ | — | Deferred: maze editor/import-export, themed/difficulty procedural generators, difficulty metric. |

**Section 2 changes:** `mazes.ts` (pacStart-first ordering, power-pellet pacStart
skip, whole-grid `findOpenNear` fallback, `validateMaze` + `reachableFrom`);
`mazes.test.ts` (+6 tests). Gate pending local run.

### Section 3 — ghosts

| ID | Tag | Status | File | Finding |
|----|-----|--------|------|---------|
| D3.1 | bug/dup | ☑ | types.ts/env/ghostAi | x-only tunnel wrap duplicated in env + ghostAi. Extracted shared `wrapPosition(width,height,x,y)`; both now call it (x-only, y-ready signature). |
| D3.2 | bug | ☑ | ghostAi.ts:169 | Frightened ghosts now flee over `legal` (may reverse) instead of `candidates`. **Behavior change** — logged in `test_history.md` Code Change Log. |
| D3.3 | bug/edge | ✗ | ghostAi.ts:141 | In-box fallback ignores `avoidBox`; only reachable with a malformed maze (now caught by `validateMaze`). Won't-fix. |
| D3.4 | cov | ☑ | test | removeReverse: no-reverse normally + allow-reverse at dead-end. |
| D3.5 | cov | ☑ | test | Edible flee moves away from Pac (and reverses — also pins D3.2). |
| D3.6 | cov | ☑ | test | In-box ghost steps toward `ghostHouseExit` via BFS. |
| D3.7 | cov | ☑ | test | Pinky (4-ahead), Inky (Blinky-vector), Clyde (8-tile switch). |
| D3.8 | cov | ☑ | test | Scatter phase routes to corner. |
| D3.9 | cov | ☑ | test | heatmap gradient-climb; hybrid reproducible via `env.nextRand`. |
| D3.10 | cov | ☑ | test | Free ghost won't enter a ghost-house tile (`avoidBox`). |
| D3.11 | feat | ☐ | — | Deferred: per-ghost personality/difficulty UI, Elroy speed-up, phase visualization, vertical-tunnel support. |

**Section 3 changes:** `types.ts` (+`wrapPosition`); `ghostAi.ts` (use
`wrapPosition`; flee over `legal`); `environment.ts` (use `wrapPosition`);
`ghostAi.test.ts` (+test helpers, +12 tests); `test_history.md` (D3.2 log).
Gate pending local run.

### Section 4 — env

Audited `environment.ts` (597 lines) and `observation.ts` (354 lines) against
the existing `environment.test.ts` (18 tests), `observation.test.ts` (28 tests),
and `collision.test.ts` (1 test). Stated priorities: correctness · coverage ·
features. Findings below are **listed, not yet fixed** (process pause for review).

| ID | Tag | Status | File | Finding |
|----|-----|--------|------|---------|
| D4.1 | bug | ✗ here | observation.ts:108-114 | `nearestPelletDir` `DIRS` uses rotational order (up=0,right=1,down=2,left=3) but the action space (`DIRECTIONS`) is up=0,down=1,left=2,right=3, so `nearestPelletDir=k` ≠ "take action k". **Same bug as M2 — already fixed in PR #11 (v8→v9).** Won't-fix on this branch; reconcile on rebase. See sequencing note below. |
| D4.2 | cov | ◐ | collision.test.ts | Was 1 test (the deferred D1.4). **Added 4**: eating an edible ghost (combo reward + ghost reset-to-start), power-pellet activation sets `edibleTimer` on all ghosts + resets combo, `releaseDelay>0` ghost can't catch Pac, and a positive control (released non-edible ghost on Pac's tile = death). **Cross-over swap left uncovered** — see note: under the current pac-moves-before-ghosts ordering a true swap is effectively unreachable with a chasing AI (Pac always lands on the ghost first → `sameTile`, not `crossOver`). Flagged for a dedicated analysis pass rather than a brittle test. |
| D4.3 | cov | ☑ | observation.test.ts | Added 6 behavioral tests after PR #11 merged + rebase: `nearestPelletDir` = up/down/left/right action toward a planted pellet (v9-aligned), "none" sentinel = 4, and tunnel-wrap (pellet across the wrap reached by going left). This is the assertion that would have caught M2/D4.1. |
| D4.4 | cov | ☑ | observation.test.ts | Added boundary tests: `pelletsRemainingBucket` at 0.10/0.25/0.50/0.75 (inclusive lower edges) + total=0 guard; `powerPelletsLeftBucket` for 0/1/2/5. |
| D4.5 | cov | ☑ | environment.test.ts | Added `pelletEscalation` ramp test (1×→3.5×→5.95×, total=0 guard) and two `clearPelletsTo` tests (leaves exact target count; deterministic under the seeded RNG). |
| D4.6 | bug | ☑ | environment.ts:336 | Fixed: `observe()` now excludes `releaseDelay>0` ghosts (`!g.inBox && g.releaseDelay <= 0`) for parity with the collision loop, so houseless mazes no longer show a phantom threat. Zero effect on classic-maze training (those ghosts start `inBox`). |
| D4.7 | refactor | ☐ | observation.ts:131-155 | `bfsPelletDir` inlines x-only tunnel wrap twice instead of calling the shared `wrapPosition` (D3.1). Consistency only; no behavior change. Low priority. |
| D4.8 | feat | ☐ | environment.ts:246,390 | Scatter/chase phase durations hard-coded (420/300 steps). Candidate `EnvParams` fields for tuning. Deferred. |

**Sequencing note (D4.1/D4.3 ↔ PR #11):** `observation.ts` is touched by both this
branch and PR #11 (the M2/v9 fix). The correct pellet-direction behavioral test
(D4.3) asserts v9 action-aligned semantics, which only hold after PR #11 merges.
Recommended order: **merge PR #11 → rebase this branch onto master → then add D4.3
asserting v9.** Branch-independent coverage (D4.2, D4.4, D4.5) and the D4.6 fix can
proceed now regardless.

---

## Changes made (detailed log)

Chronological, with exact files/functions and rationale. **Not yet committed**
unless noted; **not yet gated** (`typecheck`/`test`) — see Verification below.

### Section 1 — engine

**`src/engine/prng.ts`** — `SeededRng.int(maxExclusive)`
- Added guard `if (!(maxExclusive > 0)) return 0;` before the floor.
- Why: `int(0)` returned a technically out-of-range `0`; negative/NaN bounds
  returned negative/NaN indices. JS array access with those yields `undefined`
  (no throw), so the bug would surface far from its cause.
- The `!(x > 0)` form (not `x <= 0`) is deliberate — it also catches `NaN`,
  since every comparison with `NaN` is false.

**`src/engine/prng.test.ts`** — NEW
- Reproducibility (same seed → identical sequence), divergence across seeds,
  `next()` ∈ [0,1) over 10k draws, seed-0 non-degeneracy, `int(n)` ∈ [0,n),
  the new guard (`int(0)`/`int(-5)`/`int(NaN)` → 0), and full-range coverage.

**`src/engine/types.test.ts`** — NEW
- Pins `DIRECTIONS` order, `reverseAction` = `[1,0,3,2]` (the C3 regression
  guard) + its involution property, and `actionToDirection` clamping for
  out-of-range input (the H10 guard).

### Section 2 — mazes

**`src/mazes/mazes.ts`**
- `parse()`: now computes `pacStart`/`ghostStarts` *before* power pellets and
  threads `pacStart` into `findPowerPelletPositions`. Removed the duplicate
  inline `findOpenNear` calls in the return object.
- `generateMaze()`: same reorder — `pacStart` computed before `pp`, passed in.
- `findPowerPelletPositions(grid, w, h, pacStart?)`: new optional `pacStart`
  param; skips any anchor that resolves onto `pacStart`. Keeps the existing
  dedupe (M14).
- `findOpenNear()`: added a whole-interior scan as a last-resort fallback
  before returning the (likely-wall) target tile (D2.2).
- NEW `reachableFrom(grid, sx, sy)`: BFS/flood-fill over passable tiles
  (`0` and `2`), returns reachable `y*w+x` keys.
- NEW exported `validateMaze(m): string[]`: returns structural-violation
  messages (start-on-wall, non-passable ghost start, non-open/duplicate power
  pellet, missing/invalid ghost-house exit, unreachable open tiles).

**`src/mazes/mazes.test.ts`** — +6 tests
- `validateMaze` returns `[]` for every maze in `MAZES`; explicit reachability
  filter; passable spawns; even→odd dimension bump (`generateMaze(7,20,14)` →
  21×15); D2.1 regression (pacStart never an advertised power pellet); negative
  test (corrupted maze yields violations).

### Section 3 — ghosts

**`src/engine/types.ts`** — NEW `wrapPosition(width, height, x, y)`
- Single source of truth for tunnel wraparound (x-only). `height` accepted but
  unused so y-wrap can be added later without touching call sites.

**`src/ghosts/ghostAi.ts`**
- `nextPosition` now delegates to `wrapPosition` (was inline x-wrap).
- Edible-ghost flee reduces over `legal` instead of `candidates` (D3.2) — a
  frightened ghost may reverse to escape and a cornered one won't stall.

**`src/env/environment.ts`**
- `nextPosition` now delegates to `wrapPosition`; added it to the types import.

**`src/ghosts/ghostAi.test.ts`** — +12 tests + helpers
- Helpers: `openWorld`, `corridorWorld`, `mkGhost`, `mkEnv` (minimal env stub).
- removeReverse (normal + dead-end), edible flee (away + reverse), in-box BFS,
  Pinky/Inky/Clyde targeting, scatter phase, heatmap, hybrid reproducibility,
  avoidBox.

**`test_history.md`** — Code Change Log row for the D3.2 behavior change.

### Section 4 — env

**`src/env/environment.ts`** — `observe()` (D4.6)
- `activeGhosts` filter now `!g.inBox && g.releaseDelay <= 0` (was `!g.inBox`).
  Parity with the collision loop's `releaseDelay > 0 → continue` guard so a
  not-yet-released ghost on a houseless maze is no longer a phantom threat in
  the observation. Classic mazes unaffected (ghosts start `inBox`).

**`src/engine/collision.test.ts`** — +4 tests (D4.2)
- `frozenEnv()` helper (speeds 0, one hand-placed ghost). Eat-edible-ghost
  (combo reward + reset to spawn), power-pellet edibility + combo reset,
  releaseDelay ghost can't catch, released-ghost-on-tile death control.

**`src/env/observation.test.ts`** — +2 tests (D4.4)
- `pelletsRemainingBucket` boundary map + total=0 guard; `powerPelletsLeftBucket`
  none/one/many.

**`src/env/environment.test.ts`** — +3 tests (D4.5)
- `pelletEscalation` 1×→6× ramp + guard; `clearPelletsTo` target-count and
  seeded-determinism.

**`src/env/observation.test.ts`** — +6 tests (D4.3, post-PR #11 rebase)
- `bfsPelletDir` behavioral: `nearestPelletDir` equals the action that walks
  toward a planted pellet (v9-aligned up/down/left/right), "none" sentinel 4,
  and the tunnel-wrap path.

Gate (D4.2/4.4/4.5/4.6, pre-rebase): `npx tsc --noEmit` clean; `vitest` → 117.
Gate (after PR #11 rebase + D4.3): clean; **`vitest` → 124/124**.

---

## Caveats & guidance for future agents

**Read this before continuing the audit or running training.**

1. **No node toolchain in the audit sandbox.** `npm`/`node`/`npx` are absent
   where this audit was performed, so **nothing below has been run through
   `npm run typecheck && npm test`.** Run the gate locally before trusting any
   green checkmark in this doc. If a test fails, fix and update the status.

2. **`validateMaze` "every maze passes" is unverified against `arena`/`corridors`.**
   The reachability assertion runs over the hand-designed static mazes too. Static
   analysis says they're fully connected, but if either has a walled-off pellet
   pocket, `mazes.test.ts` will fail — and that failure is a *real latent bug in
   the maze layout*, not a bad test. Investigate the maze, don't delete the test.

3. **Reachability intentionally checks only `0` (pellet) tiles, not `2`
   (ghost-house) tiles.** The authentic classic maze has isolated `2`-tile tunnel
   stubs on row 13's edges; requiring all `2`-tiles reachable would false-fail.
   The true winnability invariant is "all pellet tiles reachable." Don't tighten
   this without checking the classic maze.

4. **D2.1 was fixed conservatively to protect the training baseline.** The
   *relocate* alternative would have ADDED a 4th power pellet to every procedural
   maze (env currently drops the pacStart one → only 3 placed), changing
   `powerPelletsLeft` and the `powerBucket` observation feature — silently
   invalidating the 2-ghost track in `test_history.md`. We *dropped* the phantom
   anchor instead: `powerPelletPositions` is now honest, gameplay is byte-identical.
   **If you ever want symmetric 4-corner power pellets, that is a deliberate
   env/observation-version change, not a bugfix** — bump the obs key version and
   re-baseline.

5. **Power pellets still spawn under ghost-start tiles in `arena`/`corridors`.**
   Discovered during D2.1: those mazes' ghost starts coincide with the other three
   power-pellet corners. The env does NOT filter pellets off ghost starts (only
   pacStart), so they're placed and remain until Pac eats them — functionally
   harmless (ghosts don't consume pellets) but visually odd. Left as-is to avoid
   a gameplay/baseline change. Logged here, not fixed.

6. **`findOpenNear` can still return a wall tile only if the grid has zero open
   tiles** (truly degenerate). `validateMaze` would flag it. All real callers are
   safe now.

7. **D1.4 (collision test expansion) is deferred to Section 4 (env).** It's an
   env-behavior test that happens to live in `engine/collision.test.ts`. Do it
   alongside the environment audit so the assertions match real `step()` behavior.

8. **D1.5 (nominal `Action` type) is cross-section.** It touches env/rl/UI action
   plumbing. Don't attempt it as a Section-1-local change; schedule it as its own
   pass once the per-section audits are done.

9. **Finding-ID scheme:** this audit uses `D<section>.<n>`. The prior reviews used
   `C/H/M/L` (see `CODE_REVIEW_2026-05-17.md`) and `N1–N21` (commit messages only).
   When you fix something, keep the ID in the commit message and test comment so
   the chain stays greppable.

---

## Verification status

| Section | typecheck | test | committed |
|---------|-----------|------|-----------|
| 1 engine | ☑ pass | ☑ pass | ☑ PR #10 |
| 2 mazes | ☑ pass | ☑ pass | ☑ PR #10 |
| 3 ghosts | ☑ pass | ☑ pass | ☑ PR #10 |
| 4 env (D4.2/4.3/4.4/4.5/4.6) | ☑ pass | ☑ pass | ☐ pending commit |

Gate run locally on branch `deep-dive-audit-2026-05-30` (2026-05-30):
`npx tsc --noEmit` clean; `npx vitest run` → **108/108 passing** (9 files).
Caveat #1 (no toolchain in audit sandbox) is now resolved for Sections 1–3.

---

## N-findings index (resolved, from commit log)

| ID | Fix | Commit |
|----|-----|--------|
| N2/N4/N7/N9 | regression tests | 0b4a71f |
| N16–N21 | fixes + tests | 0b4a71f |
| (C/H/M/L) | see CODE_REVIEW_2026-05-17.md | 1c2b1ca…f1e2374 |
